*,
*::before,
*::after {
  margin: 0;
  box-sizing: border-box;
}

html::-webkit-scrollbar {
  @apply bg-gray-900;
  width: 6px;
  height: 8px;
  display: none;
}

html::-webkit-scrollbar-thumb {
  @apply bg-gray-200;
  border-radius: 10px;
  overflow: hidden;
  display: none;
}

.wrapper {
  @apply mx-auto max-w-[1280px];
}

.blog-content {
  @apply mx-auto max-w-[1280px];

  & > img {
    @apply mx-auto h-auto w-full object-cover;
  }

  & > *:not(img) {
    @apply mx-auto max-w-[610px];
  }

  & > ul,
  & > ol {
    @apply mb-4 list-outside space-y-3;
  }

  & > strong {
    @apply font-medium;
  }

  & > ul {
    @apply list-disc;
  }

  & > ol {
    @apply list-decimal;
  }

  & > p {
    @apply mb-4;
  }

  > h2,
  > h3 {
    @apply mt-8 mb-4 text-gray-900;
  }

  > h2 {
    @apply text-title;
  }

  > h3 {
    @apply text-title;
  }
}
