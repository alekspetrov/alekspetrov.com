---
import Card from "./Card.astro";
const { posts } = Astro.props;
---

<ul class="space-y-5">
  {
    posts.map((post) => (
      <li>
        <Card title={post.data.title} link={`/blog/${post.slug}`} />
      </li>
    ))
  }
</ul>
