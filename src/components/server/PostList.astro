---
import SinglePostRow from "./SinglePostRow.astro";

const { posts } = Astro.props;
---

<ul class="space-y-5">
  {
    posts.map((post) => (
      <li>
        <a href={post.url}>
          <SinglePostRow post={post} />
        </a>
      </li>
    ))
  }
</ul>
